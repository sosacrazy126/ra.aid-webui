[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --cov=webui --cov-report=term-missing
markers =
    asyncio: mark test as async/await test
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning 